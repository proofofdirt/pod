# Contributor Dashboard (developers, supervisors, Agent Management)

- Bounty board from off-chain registry (GitHub issue = source of truth)
- GitHub OAuth; payouts by direct USDC/SOL transfer from the DAO treasury multisig (tx sig = record)
- Agent Management view: verified field submissions per agent -> ETB payroll report for Turmi
- Field agents do NOT use this app and never hold wallets
